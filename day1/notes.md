
INTRODUCTION TO PYTHON int(Integer):it stores numbers like a=10 or b=-5 float:stores decimal values like a=10.5 b=5.5 string(str): stores text enclosed in single or double quotes boolean(bool): just checks with the logical statements condition statements like if, else, elif if statement and else statement is like u give one order if it is true it gives the output or it goes with the else statement and prints the ouput elif defines like it checks with if statement or goes with the elif statement and then checks with else statement basically its like a adding a step between if and else looping statements:while, for loop, do loop while loop:it works with no limitations for loop: it checks with each steps and then runs for a limited stops do loop:first it does the statement given and then does this loop we succssfully did the challenge
