pfunction- it is used to peform a specific task that is reusable 
syntax-def function_name():
return-it is going to retuen the statement but not print the output but also add the data in the return
syntax-def square(num):
return num*num
  list- it stores multiple varialbe in it 
syntax-fruits=["apple","banana","grapes"]
  to add just print[1],to add at the end just add append,to insert at a particular point use syntax(),remove the list just use pop()
